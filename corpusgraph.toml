# Sources for shirokoff.ca. Paths are relative to this file, so the module
# stays portable — nothing here knows where it is checked out.

[[sources]]
type     = "html_blog"
path     = "../../blog"
url_base = "/blog/"

[[sources]]
type = "radar_scorecards"
path = "../../architecture-radar/scorecards.json"

# The scores live in scorecards.json; the dated calls live here — which ring an
# entry sits in, in which edition, and every move between rings.
[[sources]]
type = "radar_entries"
path = "../../architecture-radar/radar.json"

# Curated entity packs: model-drafted, human-reviewed, one file per technology.
# Unsigned packs are skipped; see knowledge-base/entities/AUTHORING.md.
[[sources]]
type     = "entity_packs"
path     = "../../knowledge-base/entities"
url_base = "/knowledge-base/entities/"

[ontology]
aliases = "../../knowledge-base/aliases.toml"
