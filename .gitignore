# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/
.coverage
htmlcov/

# Build output — the graph is generated, never committed inside this package.
# (In the site repo the artifact lives at knowledge-base/graph.json.)
graph.json
*.cache.json
.cache.json
.corpusatlas_cache.json
tiles/
viewer/tiles/
uv.lock
